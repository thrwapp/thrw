# HANDOFF: docs/roadmap.md status refresh (#116)

## What was done

Refreshed `docs/roadmap.md`'s status snapshot, M1/M3/M4 (per the issue's
explicit list), and one factually-stale paragraph in M2 (per criterion 6's
allowance to fix M2/M5-M10 text "if you find something in this issue's
own research that's factually wrong there too" — see "M2: the one
exception" below). Every claim is cited against real files/PRs, verified
directly (`git log --oneline --all -- <path>`, reading the actual current
source), not asserted from the issue body alone. No file other than
`docs/roadmap.md` was touched.

## Acceptance criteria, with evidence

**1. Status snapshot updated, "none of the actual product exists yet"
replaced.** New paragraph at the top, dated 2026-09-18, names the actual
current state of every package/service in one pass, with the old 09-15
snapshot's text removed rather than kept alongside (git history already
preserves it).

**2. M1 marked complete with file/PR citations; text updated if stale.**
All four items marked ✅ with real PR numbers, found via
`git log --oneline --all -- packages/protocol packages/relay-core`: #30
(topic builders/node types), #54 (state machine), #62 (device registry),
#65 (MQTT client), #43 (priority engine). Also noted `RelayMqttClient`
grew `subscribeAllEvents` (#97) beyond M1's original single-heartbeat
scope, and that the priority engine has since been proven end-to-end
against a real broker (#113/#114) — both true, neither invented.

**3. M3's two adapter items updated; adapter-mac in progress cites #112;
M3.3 noted done in spirit with real hardware validation outstanding.**
Android: marked substantially done, citing #74/#83/#86/#96/#102/#109/#117
(all verified via `git log -- packages/adapter-android`). Mac: marked
in-progress, citing #95/#101/#112, with the two remaining gaps named
explicitly (composition root, trigger detection) rather than just
"in progress" left vague. M3.3: kept the issue's own "done in spirit at
the relay-core layer (#113/#114)" framing, and added that #118 (found
during this same session, not anticipated by #116's original text) means
a real end-to-end path is now blocked only on adapter-mac's gaps, not on
relay-core itself.

**4. M4's adapter-linux line updated (#108); adapter-ipad's research
blocker noted, linking the (now-resolved) research issue.** Linux: ✅
bootstrap done, citing #108, explicitly named as "the same stage
adapter-mac was at after #95" for a concrete sense of how far along it
is. iPad: the full finding from #115 (merged - `docs/spec/architecture.md`'s
"iPad's Bluetooth audio-route limitation" section) summarized here too,
with the concrete implication for this milestone's acceptance-criteria
shape ("observe + prompt," not "connect/disconnect").

**5. "Suggested immediate next step" replaced with the real next step,
not deleted.** Now names the two concrete `packages/adapter-mac` gaps
(composition root, trigger detection) as the most direct path to M3's
actual goal, with real precedent to cite (#96, #86) for whoever picks
them up, plus the human-only QA step that follows and the two
lower-priority M4 items now unblocked/scoped.

**6. M2/M5-M10 otherwise untouched, except where this issue's own
research found something factually wrong.** M5-M10: byte-for-byte
unchanged — confirmed via `git diff` showing no hunks in those sections.
M2: see below.

## M2: the one exception, and why

M2's original text described the EMQX broker as still-future work
("Needs real GCP infra decisions... that should go through a human first
pass") and said nothing about relay-core actually running against it.
Both are wrong as of this research: the broker has been live since
#80/#81 (with post-deploy fixes #92/#93), and — found in this same
session, immediately before this issue — #118 wired
`PriorityEngine`/`DeviceRegistry` into an actual process subscribing to
that broker. Criterion 6 explicitly allows fixing M2 text found to be
factually wrong along the way; this qualifies on both points, not just
one. The section is now marked "mostly done," names both landed pieces
with citations, and keeps exactly one open item (production deployment
wiring for #118's process, plus TLS - pointing at #119) rather than the
prior "needs real GCP infra decisions" framing that no longer describes
reality.

## Verification

No test command applies - this is a documentation-only change.
`git status`/`git diff --stat` confirm the diff is exactly one file,
`docs/roadmap.md`.

## Known gaps / judgment calls

- **Issue numbers cited throughout were verified against real `git log`
  output**, not the issue body's own (occasionally approximate) framing
  - e.g. the issue's context section says adapter-mac wiring is merely
  "in progress" via #112, but #112 is fully merged (confirmed:
  `packages/adapter-mac/Sources/AdapterMac/MacNode.swift` exists on
  `main`) - "in progress" now more accurately describes the *adapter as
  a whole* (still missing a composition root + triggers), not that PR
  specifically, and the roadmap text reflects that distinction rather
  than repeating the issue body's phrasing uncritically.
- **#118 and #115 weren't mentioned in #116's own issue body as resolved**
  (the body says "see the relay-hosted wiring issue" / "the CoreBluetooth
  research issue" as if still open) - both were done in this same
  session immediately beforehand and are cited here by their real,
  resolved outcomes rather than left as forward-looking placeholders.
  Worth knowing if this roadmap PR is reviewed out of order relative to
  those two merging.
- No ADR implications - this is a status document, not a spec or
  contract.
