# 277 (decision 1) — relay-side command coalescing

ADR 0020 decision 1 only. Decisions 2 and 3 are untouched and still open on
#277 — decision 2 remains blocked on ADR 0014 being resolved, decision 3
(reconnect jitter) is adapter-side and self-contained.

## What this is, and what it is not

`superseded_by_newer_command` existed before this and is a **different
mechanism**: ADR 0018's sequence gate, running adapter-side, where the node
receives the stale command and discards it by sequence number. Decision 1 is
the relay not sending it at all. The telemetry being present was never
evidence this decision was done, and both are wanted — the sequence gate is
still the backstop for anything coalescing misses, including a command already
in flight over MQTT.

## The window figures, which are the part a reviewer cannot check by reading

`DEFAULT_RELEASE_WINDOW_MS = 3000` (`command-coalescer.ts:80`) and
`DEFAULT_CLAIM_WINDOW_MS = 6000` (`:89`).

Both come from #254's measurement (`docs/handoffs/254.md`), taken from both
adapters' own `command_outcome` reports rather than from two unsynchronised
clocks — which is what made the issue's earlier ~10s figure wrong:

| leg | measured |
| --- | --- |
| Mac release, command → outcome `succeeded` | **~2.2s** |
| Pixel claim, command → outcome `succeeded` | **~4.9s** |

- Release side stays at the ADR's original ~3s, now with evidence rather than
  inheritance: it covers the measured 2.2s.
- Claim side is 6000, not the 3000 the unamended ADR would have given it and
  not 5000. 5000 would sit ~2% above a single measurement, so any device
  slower than the one sample would fall outside its own window. 6000 is ~22%
  headroom over 4.9s and also covers the +5s media re-fire the ADR's amendment
  recorded.

**What I did not verify:** these are one measurement, on one pair of devices,
with no distribution behind it — #254's own handoff says exactly that. I did
not re-measure. The headroom exists because of that, and both windows are
constructor options (`relay-service.ts:390` area, `claimWindowMs` /
`releaseWindowMs`) so a real distribution can replace a judgement without a
code change.

## A steady single trigger pays nothing

Leading-edge dispatch, not trailing. The first command for an idle key
publishes synchronously; only commands arriving while a previous one is still
settling are held (`command-coalescer.ts:187`). A trailing debounce would have
added the window to every ordinary switch, spending ADR 0007's 3.5–4s p95
claim budget to fix a case that happens rarely.

Evidence it is actually free: `command-coalescer.test.ts:80` asserts a lone
submit dispatches immediately **and** schedules no timer at all, and the
entire pre-existing relay-hosted suite passes unchanged apart from the one
expectation discussed below.

## The design changed mid-implementation, and a test is why

**Only claims are ever held. A release always dispatches at once.**
(`command-coalescer.ts:27`, and the `kind === "release"` branch at `:187`.)

This narrows ADR 0020's "coalesces claim/release commands", and it is the most
important thing in this change to review.

I first implemented it symmetrically, as the ADR is written. That fails
relay-hosted's `publishes CLAIM to the first holder, then RELEASE+CLAIM on a
real handoff` test, and the failure is not a test artefact — it is ADR 0002.
Handoff is **sequential**: the winning device cannot take the route until the
losing device lets go. So a held release does not damp a burst, it stalls the
handoff and the claim queued behind it. A switch that ordinarily takes ~4.9s
would have taken ~11s whenever a second device wanted the headset within 6s of
the first getting it — which is an ordinary thing for a person to do.

It is also right on its own terms: a release means the user has moved on, so
executing it late is never what they wanted (#254's handoff makes the same
argument for why a release silences audio and never restores it), and a
release is cheap and idempotent where a claim is the slow, contended operation
whose repeated execution is the thrash the ADR is about.

**This is a deviation from the ADR's letter and is raised on #277 rather than
amended** — `docs/adr/**` is human-merge.

## The hazard that nearly shipped: the re-assert

`relay-service.ts:735` routes the registration re-assert through
`dispatchImmediately` (`command-coalescer.ts:251`), bypassing coalescing
entirely. This is load-bearing, not a convenience.

That command goes to a node that has **restarted and lost its own state**. Run
through `submit`, a claim to a node the relay had recently claimed would be
dropped as `already_in_target_state`, leaving the relay believing the node
holds the route while the node believed it held nothing — a silent desync
introduced by the very change meant to reduce churn.

**Mutation-checked rather than argued.** Replacing `dispatchImmediately` with
`submit` fails two tests: the new
`still re-asserts a claim to a node that restarts inside the window`
(`relay-service.test.ts:763`) and the **pre-existing**
`re-publishes CLAIM to a node that restarts while it is still the holder`
(#173's behaviour). Without the bypass, coalescing would have silently broken
restart recovery.

## Reporting: superseded, never a failure

`command_coalesced` (`relay-service.ts:554`), via an injectable
`onCommandCoalesced` sink (`:390`), with two reasons:

- `superseded_by_newer_command` — a newer command replaced a queued one. Same
  vocabulary as ADR 0019's reason code, for a relay-side decision.
- `already_in_target_state` — the window closed and the coalesced intent
  matched what the node was last told, so there was nothing to send.

**Deliberately not folded into `command_outcome`.** ADR 0019 defines exactly
three terminal outcomes — succeeded, failed, timed_out — reported by the
*node* about a command it received. A coalesced command was never sent, so no
node can report on it; adding a fourth outcome would change an adapter-facing
contract and double-count against the success rate that enum exists to
measure. `relay-service.test.ts:724` asserts the ADR 0019 stream stays empty
while coalescing is happening.

## #236 and #251

- **#236** (holder oscillation): its two characterisation tests pass unchanged,
  as do relay-core's `holder-oscillation.test.ts`. Coalescing does not touch
  arbitration — it only decides when a command may go out — and the reap
  machinery #263 changed is untouched here.
- **#251** (trigger chatter): this damps the relay-side symptom of exactly that
  pattern. A node bounced claim→release→claim inside the window now gets one
  claim instead of three commands. It does not fix the adapter-side chatter and
  is not a substitute for #251's own fix.

## One behaviour change a reviewer should see

`relay-service.test.ts`'s real-broker handoff test now polls 6s rather than 2s
for one assertion: A's re-claim after an A→B→A bounce inside one second is
**held** until the release window closes. That is the coalescing window doing
its job on the exact pattern the ADR describes, so the expectation moved rather
than the behaviour being wrong — but it is a real, user-visible latency change
on bounce-back, up to 3s, and it is the trade the ADR asks for.

## Verification

`pnpm turbo test typecheck --filter=@thrw/relay-core --filter=@thrw/relay-hosted
--force` — **relay-core 62 tests** (13 new), **relay-hosted 37** (2 new),
typecheck 0 errors, 0 cached.

`--force` matters: turbo's cache is shared across worktrees and a cached run in
a fresh worktree can replay another worktree's logs.

**Not verified against the live relay.** I could not: `scripts/relay-logs.sh`
needed a `gcloud` reauth, and once that was done the deployed relay-service had
gone quiet — see the note below. So there is no production evidence that
coalescing behaves as designed on real hardware, and the ~4.9s it is built on
was measured by someone else on a different build.

## Uncertain

1. **A better trigger than a timer exists, and I did not build it.** ADR 0019's
   `command_outcome` now tells the relay when a claim actually finished. Flushing
   the pending command on the outcome, with the window only as a fallback for a
   node that never reports, would release a held claim at ~2.2s or ~4.9s instead
   of at a fixed 6s. I kept the fixed window because that is what the ADR
   specifies and because gating solely on outcomes would hang forever on a
   silent node. Worth its own decision.
2. **6000 is a judgement on one sample.** Stated above; repeating it because it
   is the number most likely to be wrong.
3. **A relay restart drops pending commands.** `stop()` clears them
   (`command-coalescer.ts:270`) and there is no persistence, so a command held
   when the process dies is simply lost. ADR 0018's reconciliation on
   re-registration is the backstop, and it already is for everything else this
   process holds in memory.
4. **Coalescing is per-node, so a two-node handoff is two independent keys.**
   Correct for dispatch, but it means nothing coordinates the pair: A's release
   and B's claim can be in different states of held/sent. I believe that is fine
   because a release is never held, so the ordering ADR 0002 needs is preserved
   — but it is an invariant resting on the release rule, not on anything
   structural.

## Note on the live relay, unrelated to this change

While trying to get production evidence I found the deployed `relay-service`
has logged **nothing since 07:04Z** (~2h): 2 registrations at startup and then
silence, while EMQX shows the Pixel still publishing at 07:54Z. EMQX also
logged a crash of the relay-service client's WebSocket process at 07:29:35Z
(`emqx_ws_connection,handle_outgoing`, `{badmatch,8112}`, clientid
`mqttjs_…`). `packages/relay-core/src/mqtt-client.ts` registers no `error`,
`close`, `offline` or `reconnect` handlers and logs nothing on connection loss,
so a relay that goes deaf says nothing about it — the #236 shape again.

Not diagnosed further and not filed: it is outside decision 1, and the fix
would land in the same file this change touches, which I did not want to
conflate. Raised with Tom.

---

# 277 (decision 3, Android) — reconnect jitter

ADR 0020 decision 3, **Android half only**. Appended to this file rather than
overwriting it: decision 1's handoff above is the other half of the same
issue and there is no reason to lose it. Decision 1 is done and merged;
decision 2 stays deferred on ADR 0014 (`Status: Proposed`), recorded on the
issue per its own acceptance criterion 6. Nothing here touches either.

macOS already had the jitter
(`packages/adapter-mac/Sources/AdapterMac/Mqtt/MQTTNIOTransport.swift:119-123`).
This is the missing Android side, and the asymmetry mattered: the event that
partitions every node at once is a relay restart, which is exactly what
synchronises every node's reconnect, so the platform with no spread is the
one that piles on.

## What changed

| file | what |
| --- | --- |
| `packages/adapter-android/app/src/main/kotlin/com/thrw/adapter/android/mqtt/ReconnectJitter.kt` | new — the policy: 0-2000ms inclusive, injectable `Random` and injectable `sleep` |
| `.../mqtt/HiveMqttTransport.kt:46-47,61-64` | the jitter and its scope are constructor-injected; the reconnect callback now re-subscribes immediately and launches the report separately |
| `.../mqtt/HiveMqttTransport.kt:153-156` | `reportReregistration()` — `jitter.await()` then `hooks.onReconnected?.invoke()` |
| `.../mqtt/HiveMqttTransport.kt:187` | `close()` cancels the transport's scope, so a pending report does not fire into a transport on its way down |

## The one real design decision: where the wait sits

The Mac jitters **before the connect attempt** (`delay + jitter`, inside its
own `reconnectLoop`). Android jitters **after the connection is back**,
immediately before the re-registration report. That is forced rather than
chosen: MQTTNIO has no automatic reconnect, so the Mac adapter owns its
reconnect loop and has a backoff to add jitter to; HiveMQ owns reconnect
timing here (`automaticReconnectWithDefaultConfig`), so the only wait this
adapter can insert is on the far side of the connect.

Same range (0-2000ms inclusive, matching `Int.random(in: 0...2000)`), same
effect on the relay — re-registrations spread across a 2s window instead of
arriving in one tick.

Two consequences of the different placement, both in Android's favour and
both deliberate:

1. **Re-subscribing is not delayed here, and on the Mac it is.** A node whose
   SUBSCRIBE has not been re-sent is deaf to claim and release while looking
   perfectly healthy — the #182 failure. Widening that window by up to 2s to
   save the relay some load would be a bad trade, so `resubscribeAll()` still
   runs synchronously on HiveMQ's connected-listener thread and only the
   report waits.
2. **Publishing is never behind the jitter**, so the manual override path
   (`ManualClaim.toggle` → `AndroidNode.emitEvent` → `MqttTransport.publish`)
   stays immediate — #277's acceptance criterion 5. Structurally true: that
   path does not go through the reconnect callback at all.

## Android was not *entirely* unjittered before this

Worth recording because it is easy to state the stronger claim and be wrong.
HiveMQ's default auto-reconnect already spreads its own backoff:
`MqttClientAutoReconnectImpl.onDisconnected` computes
`min(initial * 2^attempts, max)` and adds `±25%` of it from
`ThreadLocalRandom` (read from the 1.3.3 bytecode, not from docs). With the
1s initial delay, a first reconnect attempt lands somewhere in 750-1250ms.

That is a 500ms band on the TCP connect, not a 2000ms band on the message the
relay has to process, and it does not exist at all on the Mac side — so it
narrows the gap rather than closing it, and does not make this redundant.

## Verification

`./gradlew test` green in `packages/adapter-android` (and via
`pnpm turbo test --filter=@thrw/adapter-android`). 4 new tests in
`ReconnectJitterTest.kt`, 2 in `HiveMqttTransportTest.kt`; the whole module's
suite runs green, nothing was skipped or deleted.

**Disconfirmation check — each new test was made to fail on purpose**, since
a jitter test that passes whether or not the jitter exists is worth nothing:

| mutation | result |
| --- | --- |
| `reportReregistration()` drops `jitter.await()` | both `HiveMqttTransportTest` reconnect tests FAIL, on their `re-registered without waiting out the jitter` assertions |
| jitter moved one line up, so it delays `resubscribeAll()` too | `while the jitter is pending…` FAILS — the re-subscribe never lands and the test times out waiting for a claim |
| `nextLong(maxMillis)` instead of `maxMillis + 1` | `draws from zero to the maximum inclusive` FAILS |

All three were reverted; the final tree is the unmutated version and was
re-run green.

**Not verified:** anything on real hardware. No Pixel run, no relay restart
observed with two nodes reconnecting. The behaviour under test is a delay
before one MQTT publish, which is about as close to fully covered by unit
tests as this code gets, but "nodes actually stop piling on after a relay
deploy" is a production claim and this is not evidence for it.

## Found while working, not fixed

**`hooks.subscriptions` is never populated.** Nothing in
`packages/adapter-android/app/src/main` ever writes to it — `subscribe()`
uses `callbackFlow` and registers its subscription with HiveMQ directly,
never with `Hooks`. So `resubscribeAll()` iterates an empty map in
production today and its re-subscribe loop is exercised only by tests that
populate the map themselves (which the new one does).

Whether that is a live bug depends on something I did not verify: whether
HiveMQ's automatic reconnect re-sends subscriptions registered through the
async callback API on its own. If it does, `resubscribeAll()` is harmless
dead code; if it does not, #182's deafness fix is not actually wired up on
Android. Out of scope for decision 3 and deliberately not touched here —
raised on #277 instead.

## Uncertain / judgment calls

- **2000ms is duplicated, not shared.** `ReconnectJitter.DEFAULT_MAX_JITTER_MS`
  and the Mac's literal `0...2000` are the same constant on two sides of one
  ADR with nothing keeping them in step — the same shape as
  `SelfCooldown.DEFAULT_WINDOW_MS` / `defaultSelfCooldown` and
  `RegistrationPublisher.DEFAULT_INTERVAL_MS` / `defaultRegistrationInterval`,
  and handled the same way: a kdoc on each saying "change both together". ADR
  0022's divergence is the argument for doing better than that eventually;
  inventing a cross-language constant mechanism inside this change was not
  the place to start.
- **The value itself is inherited, not derived.** 2000ms is the Mac's number,
  chosen to match rather than measured. With two nodes any spread at all is
  sufficient; the figure would want revisiting if the node count ever got
  large enough for the window, not the randomness, to be the binding
  constraint.
- **`close()` cancels the scope before disconnecting.** A pending report is
  dropped rather than raced to completion. That is correct for the two call
  sites (`AdapterForegroundService:187` restart, `:293` `onDestroy`), which
  both discard the transport immediately afterwards.
