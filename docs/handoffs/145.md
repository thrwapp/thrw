# #145: refresh the stale roadmap status snapshot

## What was done

`docs/roadmap.md` refreshed against the actual repo state. Only that file
changed (121 insertions, 67 deletions) — no ADR, no architecture.md, per
the issue's own path restriction.

The previous snapshot was written earlier the *same day* and was already
wrong: its "Suggested immediate next step" section pointed entirely at
work that had since landed.

## Every claim verified, not transcribed

The issue asked for verification against the repo rather than trust in
its own summary. Each was checked:

| Claim in the issue | How verified | Verdict |
|---|---|---|
| adapter-mac still lacks composition root + triggers | `ls Sources/AdapterMac/Triggers/`, `Sources/AdapterMacApp/` | **Stale** — both present (#127, #128) |
| Mac triggers are "`AVAudioSession` + process watching" | `grep AVAudioSession docs/spec/architecture.md:107,133` | **Stale** — architecture.md already corrected; roadmap wasn't |
| M2's two "still open" items | `gh issue view 118/119/129` | **Stale** — all three CLOSED |
| "Suggested next step" is adapter-mac's gaps | Both closed | **Stale** — rewritten |
| M1 item 3 predates `forgetNode` (#130) | Read M1 | **No change needed** — #130 doesn't alter M1's status, so it was left alone rather than padded |
| adapter-linux / adapter-ipad status (M4) | `find packages/adapter-linux/src`, `packages/adapter-ipad/Sources` | **Still accurate** — Linux has only the BlueZ seam; iPad is one stub file. M4 untouched |

Two further corrections the issue didn't list, found while checking:

- The **M2 heading still said "mostly done"** despite all its items being
  closed. Now "✅ done for this stage".
- The **M3 heading** said "Android substantially done, Mac in progress".
  Now "both adapters code-complete, blocked on #147".

## What the next step actually is now

Rewritten to say: **#147**. Neither adapter can send MQTT credentials,
and the deployed broker runs `allow_anonymous = false`, so a fully
provisioned adapter fails at connect with `badUserNameOrPassword`. This
was found by actually running a provisioned Mac app against production
during #143 — not inferred.

The knock-on is stated plainly in the doc, because it changes how much of
M3 should be trusted: **#128, #142 and #143 have only ever been verified
against local anonymous brokers**, never the real relay. The roadmap now
says so rather than implying those are production-verified.

Also recorded: the one genuine real-world data point that does exist — a
provisioned Mac app published a real registration *and* a real `voip`
event from #127's monitor detecting an actually-running VoIP app, the
first time that path ran outside a unit test.

## Test command

No test suite covers this file. `pnpm turbo lint` reports
`0 successful, 0 total` — nothing in the repo's lint setup targets
markdown, so it is a no-op here rather than a passing check, which is
worth stating plainly rather than quoting as if it verified something.
The real verification is the citation work in the table above.

## Known gaps / judgment calls

- **Factual refresh, not a rewrite** (criterion 4). Milestone structure,
  the pipeline-lane explanations and the frozen-contract nuance are all
  untouched. M4–M10 are unchanged apart from nothing; only M2/M3, the
  snapshot header and the next-step section were edited.
- **#144 is described as "in flight" rather than done.** Its PR (#151)
  was open and unmerged at the time of writing. If it has merged by the
  time this is read, that one clause is stale — which is the recurring
  hazard with this document and the reason it keeps needing refreshing.
- **The snapshot is dated the same day as the one it replaces.** That
  looks odd but is accurate: five issues merged between the two. The
  header says so explicitly rather than hiding it, since the honest
  signal here is "this document goes stale in hours, not weeks".
- **M1 was left alone.** `PriorityEngine.forgetNode` (#130) landed after
  M1 was marked done, but it doesn't change that milestone's status, and
  the issue's own list flagged it as "probably doesn't". Adding it would
  have been padding.
