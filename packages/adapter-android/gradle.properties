# AGP refuses to build a module with androidx dependencies on the
# classpath unless this is set (#102 added androidx.activity for the
# runtime-permission flow - see app/build.gradle.kts). Jetifier stays off:
# nothing here is a pre-androidx support-library artifact.
android.useAndroidX=true
