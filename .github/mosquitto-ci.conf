# Minimal broker config for CI only. Anonymous/unauthenticated access is
# fine here because this broker is never reachable outside the job network.
# Do not reuse this file for anything internet-facing.
listener 1883
allow_anonymous true
